# Port where the server will run
PORT=3000

# Secret token required to authenticate requests from iOS Shortcuts
# Generate a secure random token (e.g., using `openssl rand -hex 32` or similar)
HEALTHKIT_API_KEY=your-super-secret-bearer-token-here

# Environment (development / production)
NODE_ENV=development
